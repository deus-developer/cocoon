# Your TON wallet address (required)
# Copy from Tonkeeper or any TON wallet
# Required
COCOON_OWNER_ADDRESS=

# Your wallet private key - Ed25519, base64 encoded (required)
# Generate with: openssl rand -base64 32
# Required
COCOON_NODE_WALLET_KEY=

# Log verbosity (0=fatal, 1=error, 2=warn, 3=info, 4=debug, 1024=never)
COCOON_VERBOSITY=3

# Number of proxy connections (more = higher throughput)
COCOON_PROXY_CONNECTIONS=1

# HTTP API access token (optional, for security)
# If set, all API requests must include ?access_hash=<value>
# Generate with: openssl rand -hex 16
COCOON_HTTP_ACCESS_HASH=
